// The PluginBase class is the base class for all plugins,
// and eventually thus the Test class.
//
// This implements subtest functionality, TAP stream generation,
// and only has the basic pass/fail assertions.

import { Base } from './base'

export class PluginBase extends Base {}
